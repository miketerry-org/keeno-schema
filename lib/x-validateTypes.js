// validateTypes.js: implements validation functions for each data type

"use strict";

const validateValue = require("./validateValue");

function validValue(data, params) {
  result = {};
  return result;
}

function validBoolean(object, params) {
  result = validValue(data, params);
  return result;
}

function validCompare(object, params) {
  result = validValue(data, params);
  return result;
}

function validDate(object, params) {
  result = validValue(data, params);
  return result;
}

function validEmail(object, params) {
  result = validValue(data, params);
  return result;
}

function validEnumerated(object, params) {
  result = validValue(data, params);
  return result;
}

function validFloat(object, params) {
  result = validValue(data, params);
  return result;
}

function validInteger(object, params) {
  result = validValue(data, params);
  return result;
}

function validMatch(object, params) {
  result = validValue(data, params);
  return result;
}

function validPassword(object, params) {
  result = validValue(data, params);
  return result;
}

function validString(object, params) {
  result = validValue(data, params);
  return result;
}

function validTime(object, params) {
  result = validValue(data, params);
  return result;
}

function validTimestamp(object, params) {
  result = validValue(data, params);
  return result;
}

module.exports = {
  validBoolean,
  validCompare,
  validDate,
  validEmail,
  validEnumerated,
  validFloat,
  validInteger,
  validMatch,
  validPassword,
  validString,
  validTime,
  validTimestamp,
};
